name: Validate change

# Die Validierung ist ausschließlich das Freigabe-Gate vor Bereitstellung.
'on':
  pull_request:
    branches:
      - R260/Bereitstellung
      - R261/Bereitstellung
      - R270/Bereitstellung
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review
permissions:
  contents: read

concurrency:
  group: validate-${{ github.repository }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate release promotion
    if: github.event.pull_request.draft == false
    uses: j520730/mtext-actions/.github/workflows/reusable-validate-release-promotion.yml@0000000000000000000000000000000000000000
    with:
      # Die Null-SHAs sperren den Entwurf bis zur kontrollierten Aktivierung der zentralen Automatisierung.
      repository_name: ${{ github.event.repository.name }}
      commit_sha: ${{ github.event.pull_request.head.sha }}
      source_branch: ${{ github.head_ref }}
      target_branch: ${{ github.base_ref }}
      automation_repository: j520730/mtext-actions
      automation_ref: 0000000000000000000000000000000000000000
